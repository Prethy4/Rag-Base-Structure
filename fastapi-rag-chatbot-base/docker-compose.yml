version: "3.9"

services:
  api:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./chroma_db:/app/chroma_db   # persist vector store
    restart: unless-stopped
